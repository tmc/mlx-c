diff --git a/mlx/backend/gpu/eval.h b/mlx/backend/gpu/eval.h
index d75b30abb..3d0b5f0ad 100644
--- a/mlx/backend/gpu/eval.h
+++ b/mlx/backend/gpu/eval.h
@@ -14,7 +14,7 @@ void init();
 void new_stream(Stream s);
 void new_thread_unsafe_stream(Stream s);
 void eval(array& arr);
-void finalize(Stream s);
+MLX_API void finalize(Stream s);
 void synchronize(Stream s);
 void clear_streams();
 
diff --git a/mlx/event.h b/mlx/event.h
index cf2d5cc7d..c2caebb08 100644
--- a/mlx/event.h
+++ b/mlx/event.h
@@ -5,12 +5,13 @@
 #include <memory>
 #include <stdexcept>
 
+#include "mlx/api.h"
 #include "mlx/error.h"
 #include "mlx/stream.h"
 
 namespace mlx::core {
 
-class Event {
+class MLX_API Event {
  public:
   Event() {};
   explicit Event(Stream stream);
diff --git a/mlx/random.cpp b/mlx/random.cpp
index 2932b3eaf..599c33f34 100644
--- a/mlx/random.cpp
+++ b/mlx/random.cpp
@@ -461,7 +461,7 @@ array categorical(
     const std::optional<array>& key /*= nullopt */,
     StreamOrDevice s /* = {} */) {
   axis = get_valid_axis(axis, logits_.ndim());
-  auto logits = expand_dims(logits_, -1);
+  auto logits = expand_dims(logits_, -1, s);
   auto shape = logits.shape();
   shape.erase(shape.begin() + axis);
   shape.back() = num_samples;
diff --git a/tests/random_tests.cpp b/tests/random_tests.cpp
index 9dfe98a5d..9ccd84a66 100644
--- a/tests/random_tests.cpp
+++ b/tests/random_tests.cpp
@@ -2,6 +2,10 @@
 
 #include <numeric>
 
+#include <functional>
+#include <unordered_set>
+#include "mlx/primitives.h"
+
 #include "doctest/doctest.h"
 
 #include "mlx/mlx.h"
@@ -727,3 +731,26 @@ TEST_CASE("test laplace") {
     CHECK(abs(float(mean(out).item<bfloat16_t>())) < 0.1);
   }
 }
+
+TEST_CASE("categorical preserves explicit stream") {
+  auto stream = new_thread_unsafe_stream(Device::cpu);
+  REQUIRE(stream != default_stream(Device::cpu));
+  auto logits = array({1.f, 2.f, 3.f, 4.f, 5.f, 6.f}, {2, 3});
+  auto out = random::categorical(logits, 1, 4, random::key(0), stream);
+  int expanded = 0;
+  std::unordered_set<uintptr_t> visited;
+  std::function<void(const array&)> visit = [&](const array& a) {
+    if (!visited.insert(a.id()).second)
+      return;
+    if (a.has_primitive()) {
+      CHECK(a.primitive().stream() == stream);
+      if (a.shape() == Shape{2, 3, 1}) {
+        ++expanded;
+      }
+    }
+    for (const auto& input : a.inputs())
+      visit(input);
+  };
+  visit(out);
+  CHECK(expanded > 0);
+}
